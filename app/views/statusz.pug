extends layout

block content
  h1 Statusz
  p TODO(ricc): move to sinmple string
  p 📈app_name=#{app_name}
  p 📈DEPLOY_TARGET=#{deploy_target}
  p 📈app_version=#{app_version}
  p 📈node_env=#{node_env}

config:
  gcp:project: cicd-platinum-test032
  gcp:region: us-central1
  # This needs to be existing already or it will fail. Not sure this is a good idea, what about next stack?!?
  vanilla:existingGkeCluster: cicd-dev
  vanilla:arName: my-skaffold-cornucopia
  # vanilla:appPath: ./app
  # vanilla:errorDocument: error.html
  # vanilla:indexDocument: index.html
  # vanilla:sitePath: ./www
